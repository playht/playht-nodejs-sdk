{
  "name": "play-api-example",
  "version": "1.0.0",
  "description": "Example usage of PlayHT SDK",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "start": "yarn build && nodemon dist/server.js"
  },
  "dependencies": {
    "@playht/playht-nodejs-sdk": "file:../",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1"
  }
}
