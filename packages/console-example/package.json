{
  "private": true,
  "name": "console-example",
  "type": "module",
  "version": "0.0.1",
  "description": "Example usage for PlayHT SDK in the command line.",
  "main": "build/sdkSamples.js",
  "scripts": {
    "build": "rm -rf ./build && tsc -b",
    "start": "yarn build && node --no-warnings build/sdkSamples.js",
    "lint": "prettier --write ."
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "playht": "latest"
  },
  "resolutions": {
    "playht": "portal:../playht"
  },
  "devDependencies": {
    "@types/node": "^20.8.7",
    "prettier": "3.0.2",
    "typescript": "^5.2.2"
  }
}
