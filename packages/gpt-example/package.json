{
  "name": "gpt-example",
  "packageManager": "yarn@3.6.3",
  "scripts": {
    "install": "yarn install && cd server && yarn install && cd ../client && yarn install",
    "dev": "cd server && yarn dev"
  }
}
